# 新电脑迁移指南（macOS → Windows 整盘拷贝）

> 适用场景：把**整个项目**（含 `data/raw/` 原始数据、Git 历史等全部内容）一次性拷贝到新电脑，不依赖 GitHub 重新拉取。
> 对应迁移日期：2026-08-06

## 一、源电脑（Mac）准备

1. **找到项目文件夹**：`厦漳泉都市圈综合分析平台/`（当前位于桌面）。
2. **推荐先删除以下可重新生成的目录**（跨平台不可用或属构建产物，拷过去也没用，删掉能省约 870MB）：
   - `apps/web/node_modules/`（依赖是 Mac 专用二进制，Windows 上不可用，必须删）
   - `apps/web/dist/`（构建产物，启动时会重新生成）
   - `apps/web/.vinext/`、`apps/web/.wrangler/`（构建缓存）
   - `运行日志/`（运行时日志）
   - `.pnpm-store/`（本地包缓存）
3. **必须保留**：
   - `data/raw/`（约 821MB 原始数据，不入 Git 仓库，只能靠拷贝传递）
   - `.git/`（保留完整提交历史，新电脑上可继续 `git pull` 同步 GitHub）
   - `启动平台.bat`、`start-platform.ps1`、`启动平台.command`、README.md、docs/ 等全部代码与文档
4. `.reasonix/`、`reasonix.toml` 是本机工具状态，建议不拷（新电脑用不到）。

> 拷贝时请**复制整个文件夹**（Finder 里选中文件夹拖到移动硬盘），不要只框选里面的文件——隐藏文件（`.git/`、`.gitattributes`、`.gitignore`）也会被一并带走。

## 二、传输方式

任选其一：

- **移动硬盘 / U 盘**：空间需大于 1GB（拷前按第一步清理后项目约 1GB 以内；若不清理需约 2GB）。
- **局域网共享**：两台电脑连同一 Wi-Fi，Mac 上开启文件共享（系统设置 → 通用 → 共享 → 文件共享），Windows 上通过资源管理器 `\\Mac的IP` 访问并拷贝。
- **网盘**（百度网盘 / OneDrive 等）：压缩成 zip 上传再下载。注意 zip 别在 Mac 上右键"压缩"中文文件名时偶发编码问题，如遇乱码改用英文临时目录名。

## 三、新电脑（Windows）环境准备

1. **安装 Node.js ≥ 22.13**：https://nodejs.org/ 下载 LTS 安装包，一路默认即可。
2. **安装 pnpm**：打开 PowerShell（或 CMD）执行：
   ```powershell
   npm install -g pnpm
   ```
3. **安装 Git for Windows（可选）**：https://git-scm.com/ 默认选项安装，用于后续 `git pull` 同步。
4. **放入项目**：把拷贝的项目文件夹放到目标位置（如 `D:\Projects\`）。建议将文件夹重命名为**纯英文路径**（如 `D:\Projects\xzq-platform`），避免中文/空格路径在部分工具中出问题。

## 四、首次启动

1. 若第 1 步没删 `apps\web\node_modules`（例如直接整个拷过来了），**先在 Windows 上删除它**——Mac 装的依赖在 Windows 上无法使用。
2. 双击 `启动平台.bat`。
   - 首次运行会自动执行 `pnpm install`（需联网，约 2~5 分钟），完成后自动启动平台并打开浏览器。
   - 若 `.bat` 双击后窗口一闪而过或报错，改用 PowerShell 方式：
     ```powershell
     powershell -NoProfile -ExecutionPolicy Bypass -File .\start-platform.ps1
     ```
3. 若自动安装依赖失败，手动执行：
   ```powershell
   cd apps\web
   pnpm install
   cd ..\..
   powershell -NoProfile -ExecutionPolicy Bypass -File .\start-platform.ps1
   ```

## 五、迁移后验证

- [ ] 浏览器自动打开并显示 Local 地址（通常 `http://localhost:3000/` 或 3001，端口被占用会自动换）
- [ ] 页面各板块有数据：区域联系网络（企业关系、人口流动）、联系象限、用地分析、公共服务设施
- [ ] `data\raw\` 目录内容与 Mac 上一致（文件数、大小对比，可参考下方清单）
- [ ] 窗口关闭后平台仍在后台运行（这是正常行为，停止方式见启动窗口内提示）

## 六、后续同步更新（可选）

- 项目带 `.git` 历史且已关联 GitHub（`github.com/defy-6/xzq-analysis-platform`）：
  ```powershell
  git pull
  ```
- 之后在 Windows 上继续开发请遵循 README 中的「Git 协作开发」约定（pnpm 装依赖、LF 换行、`data/raw/` 不入库等）。

## 七、双机协作约定（Mac ↔ Windows 日常同步）

两台电脑通过 GitHub 同步代码，各自独立安装依赖。核心原则：**代码入库同步，环境各自维护**。

1. **日常同步只用一键脚本**：
   - 拉取：双击 `一键拉取.command`（Mac）/ `一键拉取.bat`（Windows）
   - 推送：双击 `一键推送.command` / `一键推送.bat`
2. **依赖自动同步（脚本已内置）**：拉取或启动时，脚本会检测 `pnpm-lock.yaml` 是否比本机 `node_modules/.modules.yaml` 新；若是（说明远端更新了依赖），会自动执行 `pnpm install`。看到"依赖需安装/更新"提示是**正常行为**，等它装完即可。
3. **不要跨机拷贝依赖与缓存**：`node_modules/`、`.wrangler/`、`.vinext/`、`node_modules/.vite`、`.pnpm-store/`、`运行日志/` 均为本机专用（已 gitignore），互相拷贝会导致启动报错；遇到问题直接删除后重新 `pnpm install`。
4. **脚本文件在 Mac 上提交**：`.command` 的可执行位只在类 Unix 系统上记录，从 Windows `git add` 新脚本会丢失该位，导致 Mac 双击报"没有权限"。新增/修改 `.command` 请在 Mac 上提交；若已丢失，在 Mac 上执行 `chmod +x *.command` 并提交修复。
5. **换行符已统一，无需手动处理**：仓库通过 `.gitattributes` 固定为 LF（`.bat` 例外用 CRLF），两侧都不要手动转换行尾，否则会产生噪音 diff。
6. **不要提交运行时日志**：`apps/web/public/data/dev-log.json`、`apps/web/开发日志.md` 由平台运行自动写入，推送前留意 `git status`，别把它们混进提交。
7. **换新机器/重装系统后首次使用**：
   - Windows：先删除拷来的 `apps\web\node_modules`（Mac 依赖不可用），双击 `启动平台.bat` 会自动重装；
   - 双击无反应时：`.command` 需有 `+x` 执行权限，`.bat` 可用 PowerShell 直跑 `start-platform.ps1` 绕过。

## 常见问题

| 现象 | 处理 |
|---|---|
| 双击 `.bat` 无反应/闪退 | 用 PowerShell 方式启动（见上），或右键 `.bat` → 编辑，用记事本另存为 CRLF 换行（保留 UTF-8） |
| `pnpm` 报 store / 模块目录相关错误 | 删除项目根目录的 `.pnpm-store` 和 `apps\web\node_modules`，重新 `pnpm install` |
| Node 版本过低报 engines 错误 | 卸载后安装 Node.js 22.13 或更高版本 |
| 端口 3000/3001 被占用 | 脚本会自动切换到空闲端口并打开真实地址，无需处理 |
| 页面有框架但数据空白 | 检查 `data\raw\` 是否完整拷贝（主要数据源在此） |
| PowerShell 提示禁止运行脚本 | 命令中已带 `-ExecutionPolicy Bypass`，如仍报错请右键 PowerShell → 以管理员身份运行一次 |

## 数据资产速览

主要数据存放位置：

- `data/raw/`：企业、人口、空间、交通、用地、公共服务原始数据（不入 Git，迁移靠手动拷贝）
- `data/reference/`：行业代码表、汇率、区县名称映射（已入库）
- `data/processed/`：模型结果与处理成果（已入库）
- `apps/web/public/data/`：网站可直接发布的数据（已入库）

详细口径见 `docs/数据资产清单.md` 与 `docs/企业关系字段与计算口径说明.md`。
