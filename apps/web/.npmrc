# 国内镜像加速：pnpm install 默认访问 registry.npmjs.org（国内常卡顿/超时）。
# 本项目依赖版本由 pnpm-lock.yaml 锁定，换镜像只影响下载速度、不影响依赖版本。
registry=https://registry.npmmirror.com/

# 下载容错（网络不稳时自动重试，避免假死）
fetch-retries=5
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=60000
