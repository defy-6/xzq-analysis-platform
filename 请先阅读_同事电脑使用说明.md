# 同事电脑使用说明（Windows）

> 本包为「厦漳泉都市圈综合分析平台」运行包：包含全部代码、已入库数据。
> 是否包含 Windows 版依赖（node_modules）取决于打包来源，请先看下面「一、包类型」。

---

## 一、包类型（先确认你收到的是哪种）

| 类型 | 打包来源 | 是否含依赖 | 同事要做的 |
|---|---|---|---|
| **完整包** `xzq-platform-win-日期.tar.gz` | Windows 电脑（`打包给同事.bat`） | ✅ 已含 Windows 版依赖 | 解压 → 配 2 个 Key → 双击启动，**零安装** |
| **无依赖包** `xzq-platform-macpack-日期.tar.gz` | Mac（`打包给同事.command`） | ❌ 不含（Mac 依赖与 Windows 不兼容） | 需先装 Node.js + pnpm（一次性，约 10 分钟），首次启动自动装依赖（联网 2~5 分钟），之后与完整包一致 |

若收到的是**无依赖包**，请先完成「二、安装运行环境（仅无依赖包需要）」，再继续后面的步骤。

## 二、安装运行环境（仅无依赖包需要）

1. 安装 Node.js ≥ 22.13：https://nodejs.org/ 下载 LTS 安装包，一路默认；
2. 打开 PowerShell 执行：
   ```powershell
   npm install -g pnpm
   ```

## 三、解压

1. 将收到的 `xzq-platform-*.tar.gz` 解压到**纯英文路径**（如 `D:\xzq-platform`）。
   Windows 10 自带 tar，在 PowerShell 中执行（路径按实际改）：

   ```powershell
   cd D:\
   tar -xzf D:\下载\xzq-platform-win-20260809.tar.gz
   ```

2. 进入解压出的 `xzq-platform` 文件夹。

## 四、配置 2 个 API Key（一次性）

打开 PowerShell，依次执行（`sk-xxx` 换成你自己的 key）：

```powershell
setx DEEPSEEK_API_KEY "sk-你的DeepSeekKey"
setx DASHSCOPE_API_KEY "sk-你的通义千问Key"
```

**配置后必须关闭并重新打开终端（或重新双击启动脚本）**，环境变量才会生效。
验证是否生效：

```powershell
echo $env:DEEPSEEK_API_KEY
echo $env:DASHSCOPE_API_KEY
```

## 五、启动平台

双击 `启动平台.bat`。

- **无依赖包**首次启动会提示「依赖需安装/更新」，自动执行 `pnpm install`（需联网，约 2~5 分钟），完成后再自动启动平台并打开浏览器；
- 若平台已在运行，重新双击会自动停止旧进程并重启到最新代码；
- 关闭启动窗口不影响平台运行（后台守护）。

## 六、后续更新同步（任选其一）

### 方式 A（推荐，轻量）：Git 同步

1. 安装 Git for Windows（https://git-scm.com/，一路默认）；
2. 之后每次更新：双击 `一键拉取.bat` —— 自动 `git pull`，若依赖有变化会自动安装，无需手动操作。

### 方式 B（零安装）：等新包

由打包方每次重新打包发全量包，解压到新目录使用（旧包内如有个人报告草稿请先备份）。

## 七、常见问题

| 现象 | 处理 |
|---|---|
| 双击 `.bat` 一闪而过/无反应 | 用 PowerShell 执行：`powershell -NoProfile -ExecutionPolicy Bypass -File .\start-platform.ps1` |
| 首次启动提示找不到 pnpm | 未完成「二、安装运行环境」：`npm install -g pnpm` 后再试 |
| AI 提示未配置 API Key | 重新执行第四步的 `setx`，并**重开终端**；确认两条命令都执行过 |
| 端口 3000/3001 被占用 | 脚本自动换端口，以窗口提示的地址为准 |
| 页面有框架但数据空白 | 检查是否解压完整（包内含 `data` 与 `apps\web\public\data` 目录） |
| 报告里部分数据缺失 | 属正常——本包未含 `data\raw` 构建源数据（平台运行不需要）；如需重新构建数据，请打包方用 `-IncludeRaw` 重新打包 |
